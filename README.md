This template is designed to deploy a single Ubuntu 22 LTS VM. It includes a virtual network with one subnet (which the VM is attached to), along with a network security group containing inbound rules to allow SSH and HTTP traffic.

This template was exported on 1/15/2026.